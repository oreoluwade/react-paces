import React from 'react';
import AddInfo from './add-info';
import './infohub.scss';

const Infohub = () => {
    return (
        <div className="info-root">
            <AddInfo />
        </div>
    );
};

export default Infohub;
